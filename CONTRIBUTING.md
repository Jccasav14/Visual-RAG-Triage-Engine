# Contributing Guidelines

- All development is committed to the `qa` branch.
- Production promotions are approved via Pull Requests on GitHub by the Principal Architect.
- Use Conventional Commits standard (`feat`, `fix`, `chore`, `docs`, `test`).
